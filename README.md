# Birthday Pages

A simple 3-page static website with:

- `index.html` for welcome text
- `song.html` for an embedded birthday audio player and song lyrics
- `photo.html` for a celebratory birthday image

Open `index.html` in your browser to view the site.
